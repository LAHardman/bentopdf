# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.npm-cache
.env
.env.production
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Coverage reports
coverage/
*.lcov

# Generated sitemap
public/sitemap.xml

# Generated by scripts/generate-security-headers.mjs at build time
security-headers.conf

# CodeQL local scan artifacts (npm run security:codeql)
codeql-db/
codeql-results.sarif
codeql-results.csv

#backup
.seo-backup
libreoffice-wasm-package

# helm chart
/bentopdf-*.tgz

# test 
dist-test
test
# Sibling checkouts of the engine/viewer forks
bentopdf-pdfium-viewer/
bentopdf-pymupdf-wasm/

# Native app projects (Capacitor) - regenerate with `npm run native:init`.
# These are build output: everything that defines them lives in
# capacitor.config.ts, assets/ and the web build.
/android/
/ios/

# Cached brotli-recompressed WASM payloads for the native builds
# (scripts/prepare-native-wasm.mjs regenerates them on demand)
/.native-cache/

# PWA side-output of `npm run native:assets` (@capacitor/assets). The native
# icons it writes into android/ and ios/ are what we want; these extras are
# unreferenced, point at ../icons/ which does not resolve from the web root,
# and would otherwise ship in the web build.
/icons/
/public/manifest.webmanifest

# ---------------------------------------------------------------------------
# Everything above is a copy of .gitignore, because EAS *replaces* .gitignore
# with this file rather than applying both - miss a line and node_modules ends
# up in the upload.
#
# Below: files that are tracked in git but that an iOS build never reads.
# ---------------------------------------------------------------------------

# Output of scripts/prepare-airgap.sh - ~49 MB of distribution tarballs.
bentopdf-airgap-bundle/

# Container and deploy assets.
Dockerfile
Dockerfile.nonroot
docker-compose.yml
docker-compose.dev.yml
cloudflare/
chart/
unraid_bentopdf.xml
nginx.conf
nginx-ipv6.sh
nginx-noindex.sh

# The Android half of the native build.
android/

# Docs and repo furniture.
docs/
signatures/
